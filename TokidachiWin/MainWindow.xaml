<Window x:Class="TokidachiWin.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    Title="Tokidachi">
    <Grid Padding="20" MinWidth="380">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <Grid ColumnSpacing="12">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <TextBlock Text="Tokidachi" FontSize="22" FontWeight="SemiBold" />
            <Button Grid.Column="1" Content="Refresh" Click="Refresh_Click" />
        </Grid>
        <ListView Grid.Row="1" ItemsSource="{x:Bind ViewModel.Providers}">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <Border Margin="0,12,0,0" Padding="12" CornerRadius="10" Background="{ThemeResource CardBackgroundFillColorDefaultBrush}">
                        <StackPanel Spacing="6">
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="{Binding DisplayName}" FontWeight="SemiBold" />
                                <TextBlock Text="{Binding Status}" Margin="12,0,0,0" Opacity="0.7" />
                            </StackPanel>
                            <ItemsControl ItemsSource="{Binding Windows}">
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <StackPanel Margin="0,4,0,0">
                                            <Grid>
                                                <TextBlock Text="{Binding Label}" />
                                                <TextBlock Grid.Column="1" Text="{Binding UsedPercent, StringFormat='{}{0:0}%'}" HorizontalAlignment="Right" />
                                            </Grid>
                                            <ProgressBar Value="{Binding UsedPercent}" Maximum="100" />
                                        </StackPanel>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                            <ItemsControl ItemsSource="{Binding Notices}">
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate><TextBlock Text="{Binding}" Opacity="0.7" TextWrapping="Wrap" /></DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                            <TextBlock Text="{Binding Message}" Foreground="Orange" TextWrapping="Wrap" />
                        </StackPanel>
                    </Border>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</Window>
